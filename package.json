{
  "name": "ai-effort-tracker",
  "displayName": "AI Effort Tracker",
  "description": "Track human vs AI effort, time, and cost per work item / branch",
  "version": "0.11.0",
  "publisher": "AlexanderErdelyi",
  "engines": { "vscode": "^1.90.0" },
  "categories": ["Other"],
  "keywords": ["copilot", "productivity", "time-tracking", "ai", "metrics"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "aiEffortTracker.showSummary",
        "title": "AI Effort Tracker: Show Session Summary"
      },
      {
        "command": "aiEffortTracker.setMode",
        "title": "AI Effort Tracker: Set Tracking Mode"
      },
      {
        "command": "aiEffortTracker.logCredits",
        "title": "AI Effort Tracker: Log Credits Used"
      },
      {
        "command": "aiEffortTracker.logChatTurn",
        "title": "AI Effort Tracker: Log a Chat Turn"
      },
      {
        "command": "aiEffortTracker.assignBranchToWorkItem",
        "title": "AI Effort Tracker: Assign Branch to Work Item"
      },
      {
        "command": "aiEffortTracker.reassignBranch",
        "title": "AI Effort Tracker: Reassign Branch to a Different Work Item"
      },
      {
        "command": "aiEffortTracker.setWorkItemEstimate",
        "title": "AI Effort Tracker: Set Work Item Estimate"
      },
      {
        "command": "aiEffortTracker.startSession",
        "title": "AI Effort Tracker: Start Tracking Session"
      },
      {
        "command": "aiEffortTracker.stopSession",
        "title": "AI Effort Tracker: Stop Tracking Session"
      },
      {
        "command": "aiEffortTracker.exportReport",
        "title": "AI Effort Tracker: Export Report (JSON)"
      },
      {
        "command": "aiEffortTracker.weeklyReport",
        "title": "AI Effort Tracker: Weekly Report (Markdown)"
      },
      {
        "command": "aiEffortTracker.exportCsv",
        "title": "AI Effort Tracker: Export Daily Activity (CSV)"
      },
      {
        "command": "aiEffortTracker.importCredits",
        "title": "AI Effort Tracker: Import Copilot Premium-Request Usage"
      }
    ],
    "configuration": {
      "title": "AI Effort Tracker",
      "properties": {
        "aiEffortTracker.idleThresholdSeconds": {
          "type": "number",
          "default": 120,
          "description": "While VS Code is focused, switch to Idle after this many seconds with no editor activity (typing, cursor moves, AI writes)."
        },
        "aiEffortTracker.codingActiveSeconds": {
          "type": "number",
          "default": 5,
          "description": "Keep counting 'Human Coding' for this many seconds after your last keystroke."
        },
        "aiEffortTracker.aiActiveSeconds": {
          "type": "number",
          "default": 5,
          "description": "Keep counting 'AI Generating' for this many seconds after the last Copilot/agent write."
        },
        "aiEffortTracker.baselineLocPerMinute": {
          "type": "number",
          "default": 5,
          "description": "Your baseline hand-coding speed (lines of code per minute) used to estimate time saved by AI."
        },
        "aiEffortTracker.hourlyRateUsd": {
          "type": "number",
          "default": 80,
          "description": "Developer hourly cost in USD, used to value the time saved (ROI)."
        },
        "aiEffortTracker.usdPerCredit": {
          "type": "number",
          "default": 0.04,
          "description": "USD cost per Copilot premium request (credit), used to estimate AI spend."
        },
        "aiEffortTracker.dailyActiveGoalMinutes": {
          "type": "number",
          "default": 240,
          "description": "Your daily focus goal in minutes, shown as a progress ring on the Focus tab."
        },
        "aiEffortTracker.autoCaptureCredits": {
          "type": "boolean",
          "default": true,
          "description": "Automatically capture the AI model used per chat/agent request by tailing the GitHub Copilot Chat log. Records an estimated premium-request weight per request (see premiumRequestMultipliers). Exact credit counts are server-side — use 'Import Copilot Premium-Request Usage' for authoritative totals."
        },
        "aiEffortTracker.autoCapturePollSeconds": {
          "type": "number",
          "default": 15,
          "description": "How often (seconds) to check the Copilot Chat log for new requests. Minimum 5."
        },
        "aiEffortTracker.autoCreditDefaultMultiplier": {
          "type": "number",
          "default": 1,
          "description": "Estimated premium-request credits to attribute to an auto-captured model request when the model is not listed in premiumRequestMultipliers."
        },
        "aiEffortTracker.premiumRequestMultipliers": {
          "type": "object",
          "additionalProperties": { "type": "number" },
          "default": {
            "gpt-4o-mini": 0,
            "gpt-4.1": 0,
            "gpt-4o": 0,
            "gpt-5-mini": 0,
            "o3-mini": 0,
            "text-embedding": 0,
            "embedding": 0,
            "gpt-5": 1,
            "claude-sonnet": 1,
            "claude-3.5": 1,
            "claude-opus": 1,
            "o1": 1,
            "gemini": 1
          },
          "description": "Estimated premium-request credits per auto-captured model request, matched by substring against the model id (longest match wins). Set a model to 0 to treat it as included/background and skip it."
        },
        "aiEffortTracker.azureDevOpsOrg": {
          "type": "string",
          "default": "",
          "description": "Azure DevOps organization URL (e.g. https://dev.azure.com/myorg)"
        },
        "aiEffortTracker.githubToken": {
          "type": "string",
          "default": "",
          "description": "GitHub personal access token (needs 'manage_billing:copilot' scope for Copilot Metrics API)"
        },
        "aiEffortTracker.githubOrg": {
          "type": "string",
          "default": "",
          "description": "GitHub organization name for Copilot Metrics API (e.g. 'myorg')"
        },
        "aiEffortTracker.githubRepo": {
          "type": "string",
          "default": "",
          "description": "GitHub repo in 'owner/repo' format for repo-level Copilot Metrics (e.g. 'myorg/myrepo')"
        },
        "aiEffortTracker.categoryRules.extensions": {
          "type": "object",
          "default": {},
          "markdownDescription": "Categorize files by extension. Keys may be `al`, `.al`, or `*.al`; values are one of `programming`, `specification`, `documentation`, `deployment`, `config`, `other`. These override the built-in defaults.",
          "additionalProperties": {
            "type": "string",
            "enum": ["programming", "specification", "documentation", "deployment", "config", "other"]
          }
        },
        "aiEffortTracker.categoryRules.folders": {
          "type": "object",
          "default": {},
          "markdownDescription": "Categorize files by folder/path. Keys are folder names or glob patterns (e.g. `docs`, `src/specs`, `**/deploy/**`); values are one of `programming`, `specification`, `documentation`, `deployment`, `config`, `other`. Folder rules take precedence over extension rules.",
          "additionalProperties": {
            "type": "string",
            "enum": ["programming", "specification", "documentation", "deployment", "config", "other"]
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.90.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {}
}
