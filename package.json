{
  "name": "ai-effort-tracker",
  "displayName": "AI Effort Tracker",
  "description": "Track human vs AI effort, time, and cost per work item / branch",
  "version": "0.16.0",
  "publisher": "AlexanderErdelyi",
  "engines": { "vscode": "^1.90.0" },
  "categories": ["Other"],
  "keywords": ["copilot", "productivity", "time-tracking", "ai", "metrics"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "aiEffortTracker.showSummary",
        "title": "AI Effort Tracker: Show Session Summary"
      },
      {
        "command": "aiEffortTracker.setMode",
        "title": "AI Effort Tracker: Set Tracking Mode"
      },
      {
        "command": "aiEffortTracker.logCredits",
        "title": "AI Effort Tracker: Log Credits Used"
      },
      {
        "command": "aiEffortTracker.importCredits",
        "title": "AI Effort Tracker: Import Real Credits from Chat Debug Export"
      },
      {
        "command": "aiEffortTracker.logChatTurn",
        "title": "AI Effort Tracker: Log a Chat Turn"
      },
      {
        "command": "aiEffortTracker.editLedgerEntry",
        "title": "AI Effort Tracker: Edit Credit Entry"
      },
      {
        "command": "aiEffortTracker.deleteLedgerEntry",
        "title": "AI Effort Tracker: Delete Credit Entry"
      },
      {
        "command": "aiEffortTracker.addManualEffort",
        "title": "AI Effort Tracker: Add Manual Effort"
      },
      {
        "command": "aiEffortTracker.editManualEffort",
        "title": "AI Effort Tracker: Edit Manual Effort Entry"
      },
      {
        "command": "aiEffortTracker.deleteManualEffort",
        "title": "AI Effort Tracker: Delete Manual Effort Entry"
      },
      {
        "command": "aiEffortTracker.addTimeEntry",
        "title": "AI Effort Tracker: Add Time Entry"
      },
      {
        "command": "aiEffortTracker.editTimeEntry",
        "title": "AI Effort Tracker: Edit Time Entry"
      },
      {
        "command": "aiEffortTracker.deleteTimeEntry",
        "title": "AI Effort Tracker: Delete Time Entry"
      },
      {
        "command": "aiEffortTracker.assignBranchToWorkItem",
        "title": "AI Effort Tracker: Assign Branch to Work Item"
      },
      {
        "command": "aiEffortTracker.reassignBranch",
        "title": "AI Effort Tracker: Reassign Branch to a Different Work Item"
      },
      {
        "command": "aiEffortTracker.moveBranchToWorkItem",
        "title": "AI Effort Tracker: Move Branch to Another Work Item"
      },
      {
        "command": "aiEffortTracker.reassignBranchesBulk",
        "title": "AI Effort Tracker: Reassign Multiple Branches to a Work Item"
      },
      {
        "command": "aiEffortTracker.deleteWorkItem",
        "title": "AI Effort Tracker: Delete Work Item"
      },
      {
        "command": "aiEffortTracker.setWorkItemEstimate",
        "title": "AI Effort Tracker: Set Work Item Estimate"
      },
      {
        "command": "aiEffortTracker.setBillableHours",
        "title": "AI Effort Tracker: Set Billable (Could-Charge) Hours"
      },
      {
        "command": "aiEffortTracker.adjustTrackedTime",
        "title": "AI Effort Tracker: Adjust Tracked Time (Correct Auto Value)"
      },
      {
        "command": "aiEffortTracker.resetTrackedTime",
        "title": "AI Effort Tracker: Reset Tracked Time to Auto"
      },
      {
        "command": "aiEffortTracker.setProjectRates",
        "title": "AI Effort Tracker: Set Project Rates (Cost & Sell)"
      },
      {
        "command": "aiEffortTracker.setDeveloperProfile",
        "title": "AI Effort Tracker: Set Developer Profile (Seniority & Baseline)"
      },
      {
        "command": "aiEffortTracker.createProject",
        "title": "AI Effort Tracker: Create Project"
      },
      {
        "command": "aiEffortTracker.linkRepoToProject",
        "title": "AI Effort Tracker: Link Current Repo to Project"
      },
      {
        "command": "aiEffortTracker.createWorkItem",
        "title": "AI Effort Tracker: Create Work Item"
      },
      {
        "command": "aiEffortTracker.editWorkItem",
        "title": "AI Effort Tracker: Edit Work Item"
      },
      {
        "command": "aiEffortTracker.assignWorkItemToProject",
        "title": "AI Effort Tracker: Assign Work Item to Project"
      },
      {
        "command": "aiEffortTracker.startSession",
        "title": "AI Effort Tracker: Start Tracking Session"
      },
      {
        "command": "aiEffortTracker.stopSession",
        "title": "AI Effort Tracker: Stop Tracking Session"
      },
      {
        "command": "aiEffortTracker.exportReport",
        "title": "AI Effort Tracker: Export Report (JSON)"
      },
      {
        "command": "aiEffortTracker.weeklyReport",
        "title": "AI Effort Tracker: Weekly Report (Markdown)"
      },
      {
        "command": "aiEffortTracker.exportCsv",
        "title": "AI Effort Tracker: Export Daily Activity (CSV)"
      },
      {
        "command": "aiEffortTracker.importCredits",
        "title": "AI Effort Tracker: Import Copilot Premium-Request Usage"
      }
    ],
    "configuration": {
      "title": "AI Effort Tracker",
      "properties": {
        "aiEffortTracker.idleThresholdSeconds": {
          "type": "number",
          "default": 120,
          "description": "While VS Code is focused, switch to Idle after this many seconds with no editor activity (typing, cursor moves, AI writes)."
        },
        "aiEffortTracker.codingActiveSeconds": {
          "type": "number",
          "default": 5,
          "description": "Keep counting 'Human Coding' for this many seconds after your last keystroke."
        },
        "aiEffortTracker.aiActiveSeconds": {
          "type": "number",
          "default": 5,
          "description": "Keep counting 'AI Generating' for this many seconds after the last Copilot/agent write."
        },
        "aiEffortTracker.baselineLocPerMinute": {
          "type": "number",
          "default": 5,
          "description": "Your baseline hand-coding speed (lines of code per minute) used to estimate time saved by AI. Tip: run 'AI Effort Tracker: Set Developer Profile' to pre-fill this from a seniority preset (still fully editable)."
        },
        "aiEffortTracker.seniority": {
          "type": "string",
          "enum": [
            "junior",
            "mid",
            "senior",
            "custom"
          ],
          "enumDescriptions": [
            "Junior — preset baseline 3 lines/min",
            "Mid — preset baseline 5 lines/min",
            "Senior — preset baseline 8 lines/min",
            "Custom — no auto-fill; use whatever 'baselineLocPerMinute' is set to"
          ],
          "default": "custom",
          "description": "Your developer seniority. Presets pre-fill 'baselineLocPerMinute' with a sensible hand-coding speed (junior=3, mid=5, senior=8 lines/min) that you can still adjust. 'custom' leaves your baseline untouched. Set this via 'AI Effort Tracker: Set Developer Profile'."
        },
        "aiEffortTracker.baselineLocPerMinuteByCategory": {
          "type": "object",
          "default": {},
          "properties": {
            "code": {
              "type": "number",
              "description": "Baseline lines/min for code files."
            },
            "spec": {
              "type": "number",
              "description": "Baseline lines/min for spec/test files."
            },
            "docs": {
              "type": "number",
              "description": "Baseline lines/min for docs/markdown files."
            }
          },
          "additionalProperties": {
            "type": "number"
          },
          "description": "Optional per-category baseline hand-coding speeds (lines/min), e.g. { \"code\": 5, \"spec\": 8, \"docs\": 12 }. When a category has no value, the flat 'baselineLocPerMinute' is used. The generated-value/ROI math currently uses the flat baseline as the effective input; per-category values are read by the profile resolver for future use."
        },
        "aiEffortTracker.hourlyRateUsd": {
          "type": "number",
          "default": 80,
          "description": "Developer hourly cost in USD, used to value the time saved (ROI). Legacy/global default cost rate: a project without its own hourly cost, and no 'defaultHourlyCostRate' set, falls back to this."
        },
        "aiEffortTracker.defaultHourlyCostRate": {
          "type": "number",
          "description": "Global default for what one developer hour COSTS, used for projects that don't set their own hourly cost. When unset, falls back to the legacy 'hourlyRateUsd'. Precedence: project setting > this > hourlyRateUsd."
        },
        "aiEffortTracker.defaultHourlySellRate": {
          "type": "number",
          "description": "Global default for what one developer hour is BILLED/SOLD for, used for projects that don't set their own sell rate. When unset, sold value is left unresolved (null) rather than assumed. Precedence: project setting > this."
        },
        "aiEffortTracker.currency": {
          "type": "string",
          "default": "USD",
          "description": "Currency the rate/ROI figures are expressed in (e.g. 'USD', 'EUR'). A project may override this in its settings."
        },
        "aiEffortTracker.creditCostPerUnit": {
          "type": "number",
          "description": "Money cost per 1 credit / premium request, used to fold AI credit spend into project cost. When unset, falls back to the legacy 'usdPerCredit'. Precedence: project setting > this > usdPerCredit."
        },
        "aiEffortTracker.usdPerCredit": {
          "type": "number",
          "default": 0.04,
          "description": "USD cost per Copilot premium request (credit), used to estimate AI spend."
        },
        "aiEffortTracker.dailyActiveGoalMinutes": {
          "type": "number",
          "default": 240,
          "description": "Your daily focus goal in minutes, shown as a progress ring on the Focus tab."
        },
        "aiEffortTracker.autoCaptureCredits": {
          "type": "boolean",
          "default": true,
          "description": "Automatically capture the AI model used per chat/agent request by tailing the GitHub Copilot Chat log. Records an estimated premium-request weight per request (see premiumRequestMultipliers). Exact credit counts are server-side — use 'Import Copilot Premium-Request Usage' for authoritative totals."
        },
        "aiEffortTracker.autoCapturePollSeconds": {
          "type": "number",
          "default": 15,
          "description": "How often (seconds) to check the Copilot Chat log for new requests. Minimum 5 (the real-credit capture uses this too, with a minimum of 3s so it can catch the transient exact-cost payload before VS Code compacts it away)."
        },
        "aiEffortTracker.autoCaptureRealCredits": {
          "type": "boolean",
          "default": true,
          "description": "Automatically capture REAL per-request chat tokens (promptTokens/completionTokens + resolved model) from VS Code's chat session storage and log them to credits (issue #59). Credits are estimated per-model from tokens via aiuRatesOverride, and UPGRADED to the exact AIU cost when the fast poll catches a request's transient copilot_usage.total_nano_aiu before it is compacted away. The GitHub billing import stays the authoritative reconciliation source. NOTE: this live estimator is automatically DISABLED when 'creditImportFolder' is set, because exact Chat Debug imports then become the source of truth (exact-only mode)."
        },
        "aiEffortTracker.creditImportFolder": {
          "type": "string",
          "default": "",
          "description": "Path to a folder watched for Copilot Chat Debug exports (issue #70). Export the Chat Debug log ('all prompts') as JSON into this folder and the extension auto-imports the EXACT per-request AIU cost (copilot_usage.total_nano_aiu) so credit totals match GitHub billing exactly. Imports are deduped/idempotent per chat turn (by promptId), and a later, more complete export upgrades the value. When this is set, EXACT-ONLY mode is active: the live token-rate estimators are disabled and their estimated 'auto' credit rows are removed so they cannot double-count. Leave blank to use the live estimators instead. Imported credits are attributed to the current branch/work item (re-assignable via the ledger row editor)."
        },
        "aiEffortTracker.aiuRatesOverride": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "inputNanoAiuPerToken": { "type": "number" },
              "outputNanoAiuPerToken": { "type": "number" }
            }
          },
          "default": {},
          "description": "Optional overrides for the per-model AIU rate table used to ESTIMATE real-credit cost from tokens (issue #59). Keys are model-family stems matched by substring against the model id (longest match wins), e.g. 'claude-opus', 'gpt-5', 'gemini'. Each value is { inputNanoAiuPerToken, outputNanoAiuPerToken } in nano-AIU per token (1 AIU = 1e9 nano-AIU). Unset families fall back to the built-in table, then a safe default; the exact total_nano_aiu capture always overrides an estimate when available."
        },
        "aiEffortTracker.autoCreditDefaultMultiplier": {
          "type": "number",
          "default": 1,
          "description": "Estimated premium-request credits to attribute to an auto-captured model request when the model is not listed in premiumRequestMultipliers."
        },
        "aiEffortTracker.premiumRequestMultipliers": {
          "type": "object",
          "additionalProperties": { "type": "number" },
          "default": {
            "gpt-4o-mini": 0,
            "gpt-4.1": 0,
            "gpt-4o": 0,
            "gpt-5-mini": 0,
            "o3-mini": 0,
            "text-embedding": 0,
            "embedding": 0,
            "gpt-5": 1,
            "claude-sonnet": 1,
            "claude-3.5": 1,
            "claude-opus": 1,
            "o1": 1,
            "gemini": 1
          },
          "description": "Estimated premium-request credits per auto-captured model request, matched by substring against the model id (longest match wins). Set a model to 0 to treat it as included/background and skip it."
        },
        "aiEffortTracker.azureDevOpsOrg": {
          "type": "string",
          "default": "",
          "description": "Azure DevOps organization URL (e.g. https://dev.azure.com/myorg)"
        },
        "aiEffortTracker.githubToken": {
          "type": "string",
          "default": "",
          "description": "GitHub personal access token (needs 'manage_billing:copilot' scope for Copilot Metrics API)"
        },
        "aiEffortTracker.githubOrg": {
          "type": "string",
          "default": "",
          "description": "GitHub organization name for Copilot Metrics API (e.g. 'myorg')"
        },
        "aiEffortTracker.githubRepo": {
          "type": "string",
          "default": "",
          "description": "GitHub repo in 'owner/repo' format for repo-level Copilot Metrics (e.g. 'myorg/myrepo')"
        },
        "aiEffortTracker.categoryRules.extensions": {
          "type": "object",
          "default": {},
          "markdownDescription": "Categorize files by extension. Keys may be `al`, `.al`, or `*.al`; values are one of `programming`, `specification`, `documentation`, `deployment`, `config`, `other`. These override the built-in defaults.",
          "additionalProperties": {
            "type": "string",
            "enum": ["programming", "specification", "documentation", "deployment", "config", "other"]
          }
        },
        "aiEffortTracker.categoryRules.folders": {
          "type": "object",
          "default": {},
          "markdownDescription": "Categorize files by folder/path. Keys are folder names or glob patterns (e.g. `docs`, `src/specs`, `**/deploy/**`); values are one of `programming`, `specification`, `documentation`, `deployment`, `config`, `other`. Folder rules take precedence over extension rules.",
          "additionalProperties": {
            "type": "string",
            "enum": ["programming", "specification", "documentation", "deployment", "config", "other"]
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.90.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {}
}
